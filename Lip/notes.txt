(*) add command line args parsing
(*) add file work mode

(*) make string understand special characters

(*) add Future Value
(*) add Socket Value

(*) more embedded functions (
		collections - filter, map, for_each, all_of, flaten, sort, heap, set, ...,
		type checking - is_num, is_list, is_str, is_func, is_int, is_bool, ...,
		read/write to file,
		strings,
		lists,
		functionObjects,
		multi threading - async(func, args...) -> Future,
		networking - open, bind, listen, accept, connect, close, send, recv
	)

(*) add help command
(*) add warnings
(*) add check for undef of embedded function

(*) stop infinite recursion

(*) formalize language grammer

(*) remove VariableExpression
(*) clean up Environment
(*) add more Function metadata (description)
(*) clean up Function
(*) clean up embedded functions
(*) clean up Interpreter
(*) enhance List interface
(*) clean up List class

(*) more efficient environment states

(*) add saving function to file and loading them from file
(*) different embedded functions based on options

(?) replace FunctionData shr_ptr with unq_ptr

(?) add Error Value
(?) add assert emb function

(?) add value wrapper

(?) prefix embedded functions with emb_

(?) move Utils into static library

(?) add Null Value